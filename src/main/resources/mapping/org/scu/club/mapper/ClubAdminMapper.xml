<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.scu.club.mapper.ClubAdminMapper">

  <sql id="allResult">
    <trim suffixOverrides=",">
      club_admin.id,
      club_admin.username,
      club_admin.`password`,
      club_admin.club_id clubId,
      club_admin.user_id userId,
      club_admin.avatar,
      club_admin.create_time createTime,
      club_admin.update_time updateTime
    </trim>
  </sql>

  <select id="getByUserId" resultType="org.scu.club.entity.ClubAdmin">
    SELECT
      <include refid="allResult"></include>
    FROM
      club_admin
    WHERE
    user_id = #{userId}
  </select>
</mapper>